package com.sgp.repository;

import com.sgp.model.AlertaResumoConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AlertaResumoConfigRepository extends JpaRepository<AlertaResumoConfig, Long> {
}
